node_modules/
dist/
shots/
# a bench run is a real wall: screenshots and a scorecard, regenerated rather than kept
bench/
.DS_Store
*.log
dist-core/

# nothing that carries a key, and nothing a build wrote
.env
.env.*
*.local
*.tsbuildinfo
.wall-test-key

# the handoff directory is written at runtime by the app and read by an agent, in whatever
# project a design was made for. In this one that includes Wall dogfooding itself.
.wall/

# rust build output. Gigabytes of it, all of it reproducible from the crate and the lockfile.
target/
src-tauri/gen/

# the 1024px source the icon set is cut from, regenerated by node src-tauri/icon.mjs.
# The cut icons are committed, because building the app should not require running the generator.
src-tauri/icon-source.png
wall-mcp-*.tgz
.try/
marks/
marks.last/
directions/
identity/
identity.last/
motion/
motion.last/
animated/
