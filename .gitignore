node_modules/
dist-core/
.DS_Store
*.log
*.tsbuildinfo

# nothing that carries a key, and nothing a build wrote
.env
.env.*
*.local
.motioneer-test-key

# what the studio writes while it runs: the tailwind cache, the recent addresses, the chosen
# service, and the session it puts back when it restarts under you
.studio/

motioneer-*.tgz

dist-editor/
